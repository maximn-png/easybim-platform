{
  "name": "easybim-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "copy-assets": "node scripts/copy-assets.js",
    "dev": "npm run copy-assets && turbo dev",
    "build": "npm run copy-assets && turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "engines": {
    "node": ">=18"
  }
}
